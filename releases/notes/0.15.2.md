<!-- zh-Hans -->
## 更新
- 修正发布包中的匿名统计配置来源，确保正式包使用 `ping-island-global` 项目。
- Release notes 展示内容已简化，只保留这次更新最关键的信息。

## 说明
- 匿名统计仍仅在用户同意后启用，并继续使用 allowlist 字段。

<!-- en -->
## Updates
- Fixed the anonymous telemetry configuration source for release builds so production packages use the `ping-island-global` project.
- Simplified the release notes shown to users so only the key update details remain.

## Notes
- Anonymous telemetry still only runs after user consent and continues to use allowlisted fields.
